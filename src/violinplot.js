const colorScale = require('./colorScale');

/**
 * Create a violinplot from the dataframe 
 * @param {object} data 
 * @param {object} params x, y, color, palette, makerStyle, size, xlab, ylab, highlight
 * @example violinplot(data, {x: "Sepal.Length", y: "Sepal.Width", color: "Species", palette: ["red", "green", "blue"]} );
 */
let violinplot = function(data, params) {

}

module.exports = violinplot;