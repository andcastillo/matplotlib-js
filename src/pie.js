const colorScale = require('./colorScale');

/**
 * Create a pie from the dataframe 
 * @param {object} data 
 * @param {object} params x, y, color, palette, makerStyle, size, xlab, ylab, highlight
 * @example pie(data, {x: "Sepal.Length", y: "Sepal.Width", color: "Species", palette: ["red", "green", "blue"]} );
 */
let pie = function(data, params) {

}

module.exports = pie;