const colorScale = require('./colorScale');

/**
 * Create a tree from the dataframe 
 * @param {object} data 
 * @param {object} params x, y, color, palette, makerStyle, size, xlab, ylab, highlight
 * @example tree(data, {x: "Sepal.Length", y: "Sepal.Width", color: "Species", palette: ["red", "green", "blue"]} );
 */
let tree = function(data, params) {

}

module.exports = tree;