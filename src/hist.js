const colorScale = require('./colorScale');

/**
 * Create a histogram from the dataframe 
 * @param {object} data 
 * @param {object} params x, y, color, palette, makerStyle, size, xlab, ylab, highlight
 * @example hist(data, {x: "Sepal.Length", y: "Sepal.Width", color: "Species", palette: ["red", "green", "blue"]} );
 */
let hist = function(data, params) {

}

module.exports = hist;